<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- action window -->
        <record id="action_res_partner" model="ir.actions.act_window">
            <field name="name">Candidate</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.partner</field>
            <field name="view_mode">kanban,tree,form,graph</field>
            <field name="context">{"default_is_candidate":1}</field>
            <field name="domain">[('is_candidate','=',True)]</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                Click to add a new Candidate
                </p><p>
                Click the Create button to add a new Candidate
                </p>
            </field>
        </record>

        <menuitem id="menu_res_partner" name="Candidate" parent="vit_election_app.vit_election_app_config_menu" action="action_res_partner" sequence="30"/>


    </data>
</odoo>